{
  "name": "sugar-pretzels",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ vitarico1/sugar-pretzels",
    "create-local": "graph create --node http://localhost:8020/ vitarico1/sugar-pretzels",
    "remove-local": "graph remove --node http://localhost:8020/ vitarico1/sugar-pretzels",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 vitarico1/sugar-pretzels"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.4",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
